[deploy]
startCommand = "/bin/sh -c \"export MCP_TRANSPORT=http MCP_HOST=0.0.0.0 MCP_PORT=$PORT && exec python main.py\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
sleepApplication = false
